## IDF Component Manager Manifest File
dependencies:
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
  espressif/esp_websocket_client:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=5.0"
